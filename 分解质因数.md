#### 分解质因数（统计质因数次数版本）
```cpp
int a[10010];

void decompose(int x) {
	for (int i = 2; i <= x / i; i++ ) {
		while(x % i == 0) a[i]++, x /= i;
	}
	if(x > 1) a[x]++;
}
```

```cpp
	vector<int> a(N);
	auto decompose = [&](int x) -> void {
		for (int i = 2; i <= x / i; i++) {
			while(x % i == 0) a[i]++, x /= i;
		}
		if(x > 1) a[x]++;
	};
```

```cpp
	map<int, int> mp;
	auto decompose = [&](int x) -> void {
		for (int i = 2; i <= x / i; i++) {
			while(x % i == 0) mp[i]++, x /= i;
		}
		if(x > 1) mp[x]++;
	};
```