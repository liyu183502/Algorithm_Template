```cpp
int vis[N], prim[N], cnt;

void get_prim(int n)
{
	for (int i = 2; i <= n; i++ )
	{
		if(!vis[i]) prim[++cnt] = i;
		for (int j = 1; i <= n / prim[j]; j++ )
		{
			vis[prim[j] * i] = 1;
			if(i % prim[j] == 0) break;
		}
	}
}
```