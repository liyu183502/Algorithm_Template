## 单文件
```cpp
#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long
#define pii pair<int,int>
using i128 = __int128;
const int inf = 4e18;
const int p = 998244353;
const int N = 2e5 + 5;
int dx[] = {-1, 0, 1, 0, -1, 1, 1, -1};
int dy[] = {0, 1, 0, -1, 1, 1, -1, -1};
const int MAX_CASE = 2000000;
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
int rnd(int l, int r) {
	return uniform_int_distribution<int>(l, r)(rng);
}

// 正解
int solve(vector<int> a) {
	int ans = 0;

	return ans;
}

// 暴力
int bf(vector<int> a) {
	int ans = 0;


	return ans;
}

signed main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	for (int test = 1; test <= MAX_CASE; test++) {
		if(test % 100000 == 0) cout << "test=" << test << endl;
		int n = rnd(1, 100000);

		vector<int> a(n + 1);
		for (int i = 1; i <= n; i++) {
			a[i] = rnd(-1000000, 1000000);
		}

		int ans1 = solve(a);
		int ans2 = bf(a);

		if(ans1 != ans2) {
			cout << "test = " << test << endl;

            // 输出生成数据
			cout << "n = " << n << endl;
			cout << "a = ";
			for (int i = 1; i <= n; i++) {
				cout << a[i] << " ";
			}
			cout << endl;

			cout << "std = " << ans1 << endl;
			cout << "bf  = " << ans2 << endl;

			break;
		}
	}
	return 0;
}
```


## 数据生成器
```cpp
#include <bits/stdc++.h>
using namespace std;

mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());

int rnd(int l, int r) {
	return uniform_int_distribution<int>(l, r)(rng);
}

int main() {
	int n = rnd(1, 10);

	cout << n << endl;

	for (int i = 1; i <= n; i++) {
		cout << rnd(1, 20) << " ";
	}
	cout << endl;

	return 0;
}
```
