#### 二分模板
注意点：1开区间，2结果是l或r，3lambda函数里每次都要重置数组和变量
```cpp
//找最大值
int l = 0, r = inf;
while(l + 1 < r){
    int mid = (l + r) >> 1;
    if(check(mid)) r = mid;
    else l = mid;
}
cout << r << endl;

//找最小值
int l = 0, r = inf;
while(l + 1 < r){
    int mid = (l + r) >> 1;
    if(check(mid)) l = mid;
    else r = mid;    
}
cout << l << endl;
```
#### 二分答案，并且答案包含0，以及存在ans不存在情况
```cpp
int l = -1, r = inf, ans = -1;
while(l + 1 < r){
    int mid = (l + r) >> 1;
    if(check(mid)) {
        ans = mid;
        r = mid;
    }
    else l = mid;
}

cout << ans << endl;
```