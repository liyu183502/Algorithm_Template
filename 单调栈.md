#### 注意事项：
1.反方向的时候记得清空stack  
2.记得判断是否为空  
3.知道这个找的是第一个小（大）于的元素，注意<=是否取等于
```cpp
	stack<int> st;
	vi l(n + 1), r(n + 1);
	for (int i = 1; i <= n; i ++ ){
		while(!st.empty() && a[st.top()] >= a[i]){
			st.pop();
		}
		l[i] = (st.empty() ? 0 : st.top());
		st.push(i);
	}

	while(!st.empty()){
		st.pop();
	}

	for (int i = n; i > 0; i -- ){
		while(!st.empty() && a[st.top()] >= a[i]){
			st.pop();
		}
		r[i] = (st.empty() ? (n + 1) : st.top());
		st.push(i);
	}
```