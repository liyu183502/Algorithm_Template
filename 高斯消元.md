## 高斯消元
拓展：差分是可逆的线性变换，不改变矩阵的线性关系（秩）

### 普通线性方程
ans 为答案数组，ans里面需要判断和 eps 的大小，小于eps的输出0

0 无解，1唯一解，2无穷多解
```cpp
const long double eps = 1e-12;
int gauss(vector<vector<long double>> a, vector<long double> &ans) {
	int n = a.size();
	int m = a[0].size() - 1;
	vector<int> pos(m, -1);
	int row = 0;
	for (int col = 0; col < m && row < n; col++) {
		int mx = row;
		for (int i = row; i < n; i++) {
			if(fabs(a[i][col]) > fabs(a[mx][col])) {
				mx = i;
			}
		}

		if(fabs(a[mx][col]) < eps) continue;
		
		swap(a[mx], a[row]);
		pos[col] = row;
		for (int i = 0; i < n; i++) {
			if(i == row) continue;
			long double t = a[i][col] / a[row][col];
			for (int j = col; j <= m; j++) {
				a[i][j] -= t * a[row][j];
			}
		}
		row++;
        // row为方程的秩
        // if(row == min(n, m)) {
        //     满秩
        // }
	}

	for (int i = 0; i < n; i++) {
		bool zero = true;
		for (int j = 0; j < m; j++) {
			if(fabs(a[i][j]) > eps) {
				zero = false;
				break;
			}
		}

		if(zero && fabs(a[i][m]) > eps) {
			return 0;
		}
	}
	ans.assign(m, 0);

	for (int i = 0; i < m; i++) {
		if(pos[i] != -1) {
			ans[i] = a[pos[i]][m] / a[pos[i]][i];
		}
	}

	for (int i = 0; i < m; i++) {
		if(pos[i] == -1) {
			return 2;
		}
	}

	return 1;
}
```

## 异或方程
```cpp
const int M = 2005;

int gauss(vector<bitset<M>> a, int m, vector<int> &ans) {
	int n = a.size();
	vector<int> pos(m, -1);
	int row = 0;
	for (int col = 0; col < m && row < n; col++) {
		int p = row;
		while(p < n && !a[p][col]) {
			p++;
		}
		if(p == n) {
			continue;
		}
		swap(a[p], a[row]);
		pos[col] = row;

		for (int i = 0; i < n; i++) {
			if(i != row && a[i][col]) {
				a[i] ^= a[row];
			}
		}
		row++;
	}

	for (int i = 0; i < n; i++) {
		bool zero = true;
		for (int j = 0; j < m; j++) {
			if(a[i][j]) {
				zero = false;
				break;
			}
		}
		if(zero && a[i][m]) {
			return 0;
		}
	}
	ans.assign(m, 0);

	for (int i = 0; i < m; i++) {
		if(pos[i] != -1) {
			ans[i] = a[pos[i]][m];
		}
	}
	for (int i = 0; i < m; i++) {
		if(pos[i] == -1) {
			return 2;
		}
	}
	return 1;
}
```
---