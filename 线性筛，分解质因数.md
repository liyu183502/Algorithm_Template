#### 分解质因数（统计质因数次数版本）
```cpp
#include<iostream>
using namespace std;
int a[10010];

void decompose(int x)
{
	for (int i = 2; i <= x / i; i++ )
	{
		while(x % i == 0) a[i]++, x /= i;
	}
	if(x > 1) a[x]++;
}

int main()
{
	int x;
	cin >> x;
	decompose(x);
	for (int i = 2; i <= x; i++ ) 
		if(a[i]) cout << i << " " << a[i] << endl;
	return 0;
}
```