```cpp
	auto dfs = [&](auto && self, int u, vi & cnt, int & flag) -> void {
		for (auto v : g[u]){
			if(vis[v] + vis[u] == 3){
				continue;
			}
			else if(vis[v] == vis[u]){
				flag = 1;
				continue;
			}	

			if(vis[v] == 0){
				vis[v] = 3 - vis[u];
				cnt[vis[v]] ++;
				self(self, v, cnt, flag);
			}
		}
	};
```