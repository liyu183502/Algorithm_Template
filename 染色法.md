## 染色法，如果是二分图，答案加上颜色多的那个颜色个数
```cpp
void solve(){
    int n, m;
    cin >> n >> m;

    vector<vector<int>> adj(n + 1);
    for(int i = 1; i <= m; i++){
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        adj[v].push_back(u);
    }

	int ans = 0, flag = 1;
	vector<int> col(n + 1, -1), cnt(2, 0);
	auto dfs = [&](auto && self, int u, int c)->void{
		col[u] = c;
		cnt[c]++;
		for(int v : adj[u]){
			if(col[v] == -1){
				self(self, v, 1 - c);
			} else {
				if(col[v] == c){
					flag = false; 
				}
			}
		}
	};

    for(int i = 1; i <= n; i++){
        if(col[i] == -1){
            cnt[0] = cnt[1] = 0;
            flag = 1;
            dfs(dfs, i, 0); 
            if(flag) ans += max(cnt[0], cnt[1]);
        }
    }
    cout << ans << endl;
}
```