## 区间子数组最值
```
	auto get = [&](int l, int r) -> pii {
		int x = a[l], mn = a[l];
		int y = a[l], mx = a[l];
		for (int i = l + 1; i <= r; i++) {
			x = min(a[i], x + a[i]);
			mn = min(mn, x);

			y = max(a[i], y + a[i]);
			mx = max(mx, y);
		}
		return {mn, mx};
	};
```