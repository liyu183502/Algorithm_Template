## 区间子数组最值
```cpp
	auto get = [&](int l, int r) -> pii {
		int mn = 0, mx = 0, x = 0, y = 0;
		for (int i = l; i <= r; i++) {
			x = min(0LL, x + a[i]);
			y = max(0LL, y + a[i]);
			mn = min(mn, x);
			mx = max(mx, y);
		}
		return pii{mn, mx};
	};
```