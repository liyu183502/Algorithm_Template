## 约数
```cpp
vector<int> get_divisors(int x) {
	vector<int> d;
	for (int i = 1; i * i <= x; i++) {
		if(x % i == 0) {
			d.push_back(i);
			if(i * i != x) {
				d.push_back(x / i);
			}
		}
	}
	sort(d.begin(), d.end());
	return d;
}
```


每个数 log，需要spf
```cpp
int divisor_cnt(int x) {
	int res = 1;

	while(x > 1) {
		int p = spf[x];
		int cnt = 0;

		while(x % p == 0) {
			x /= p;
			cnt++;
		}

		res *= cnt + 1;
	}

	return res;
}
```
每个数sqrt(n)，不需要spf
```cpp
int divisor_cnt(int n) {
	int res = 1;

	for (int i = 2; i * i <= n; i++) {
		if(n % i == 0) {
			int cnt = 0;
			while(n % i == 0) {
				n /= i;
				cnt++;
			}
			res *= cnt + 1;
		}
	}

	if(n > 1) {
		res *= 2;
	}

	return res;
}
```