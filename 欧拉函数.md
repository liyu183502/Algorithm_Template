## 欧拉函数
定义：φ(n) 表示：在 1,2,…,n 中，有多少个数与 n 互质。
算法复杂度：根号 n 
## 欧拉定理
如果gcd(a,n) = 1, 那么有 a^φ(n) ≡ 1 (mod n)	​

## 费马小定理
a^(p - 1) ≡ 1 (mod p)  p 为质数，a 和 p 互质  

#### 求欧拉函数
```cpp
int phi(int n){
	int ans = n;
	for (int i = 2; i * i <= n; i++) {
		if(n % i == 0) {
			ans = ans / i * (i - 1);
			while(n % i == 0) n /= i;
		}
	}
	if(n > 1) ans = ans / n * (n - 1);
	return ans;
}
```
