## 拓扑排序  
应用场景：1.判断是否存在环，2.找出某种顺序，3.找出所有不在环内的点  
格式：入度din，图g，拓扑序数组tp
```cpp
    vector<int> tp, din(n + 1);
    queue<int> q;
    while(q.size()){
        int u = q.front();
        q.pop();
        tp.push_back(u);
        for (auto v : g[u]){
            if(--din[v] == 0) q.push(v);
        }
    }
    return tp.size() == n; 
```