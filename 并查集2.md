```cpp
struct DSU {
	vector<int> fa, sz;

	DSU() {
		fa.push_back(0);
		sz.push_back(0);
	}

	int add(int w = 1) {
		int id = fa.size();
		fa.push_back(id);
		sz.push_back(w);
		return id;
	}

	int find(int x) {
		if (fa[x] == x) return x;
		return fa[x] = find(fa[x]);
	}

	bool merge(int x, int y) {
		x = find(x);
		y = find(y);

		if (x == y) return false;

		if (sz[x] < sz[y]) swap(x, y);

		fa[y] = x;
		sz[x] += sz[y];

		return true;
	}

	int size(int x) {
		return sz[find(x)];
	}
};
```