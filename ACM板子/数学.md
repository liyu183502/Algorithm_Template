[toc]

## ksm

```cpp
int ksm(int a, int n) {
    a %= p;
    int res = 1;
    while(n){
        if(n & 1) res = a * res % p;
        a = a * a % p;
        n >>= 1;
    }
    return res % p;
}
```

---


## 线性筛

primes 存质数，cnt 质数个数，spf 存最小质因数

get_primes_factor 存所有的质因数
```cpp

int spf[N], primes[N], cnt;

void init() {
	for (int i = 2; i < N; i++) {
		if(!spf[i]) {
			spf[i] = i;
			primes[++cnt] = i;
		}

		for (int j = 1; j <= cnt && i < N / primes[j]; j++) {
			int p = primes[j];
			spf[i * p] = p;
			if(i % p == 0) break;
		}
	}
}

bool is_prime(int x) {
	return x >= 2 && spf[x] == x;
}

vector<int> get_prime_factor(int x) {
	vector<int> res;

	while(x > 1) {
		int p = spf[x];
		res.push_back(p);

		while(x % p == 0)
			x /= p;
	}

	return res;
}
 
```

---


## 分解质因数

分解质因数（统计质因数次数版本）

```cpp
	vector<int> a(N);
	auto decompose(int x) -> void {
		for (int i = 2; i <= x / i; i++) {
			while(x % i == 0) a[i]++, x /= i;
		}
		if(x > 1) a[x]++;
	};
```

---
## 约数
```cpp
vector<int> get_divisors(int x) {
	vector<int> d;
	for (int i = 1; i * i <= x; i++) {
		if(x % i == 0) {
			d.push_back(i);
			if(i * i != x) {
				d.push_back(x / i);
			}
		}
	}
	sort(d.begin(), d.end());
	return d;
}
```

每个数 log，需要spf
```cpp
int divisor_cnt(int x) {
	int res = 1;

	while(x > 1) {
		int p = spf[x];
		int cnt = 0;

		while(x % p == 0) {
			x /= p;
			cnt++;
		}

		res *= cnt + 1;
	}

	return res;
}
```
每个数sqrt(n)，不需要spf
```cpp
int divisor_cnt(int n) {
	int res = 1;

	for (int i = 2; i * i <= n; i++) {
		if(n % i == 0) {
			int cnt = 0;
			while(n % i == 0) {
				n /= i;
				cnt++;
			}
			res *= cnt + 1;
		}
	}

	if(n > 1) {
		res *= 2;
	}

	return res;
}
```
---

## 阶乘与组合数

### 大小
33的阶乘差不多i128（1e38）

18的阶乘差不多ll

```cpp
int ksm(int a, int n) {
	a %= p;
	int res = 1;
	while(n) {
		if(n & 1) res = res * a % p;
		a = a * a % p;
		n >>= 1;
	}
	return res;
}

int fact[N], infact[N];

void init() {
	fact[0] = 1;
	for (int i = 1; i < N; i++) {
		fact[i] = fact[i - 1] * i % p;
	}

	infact[N - 1] = ksm(fact[N - 1], p - 2);
	for (int i = N - 1; i >= 1; i--) {
		infact[i - 1] = infact[i] * i % p;
	}
}

int C(int n, int m) {
	if(m < 0 || m > n) return 0;
	return fact[n] * infact[m] % p * infact[n - m] % p;
}

int A(int n, int m) {
	if(m < 0 || m > n) return 0;
	return fact[n] * infact[n - m] % p;
}
```
### 质数个数
1e5 -> 一万个

1e6 -> 八万个

1e7 -> 六十六万个

---


### 性质1
$\binom{n}{m}\text{奇数}\iff m\&(n-m)=0 \iff m\&n=m$

### 性质2
$\forall\,0\le m\le n,\binom{n}{m}\text{奇数}\iff n=2^k-1$


---

## 逆元

### 线性求逆元
p 为质数

```cpp
const int N = 1e7 + 5;
const int p = 998244353;
int inv[N];

void init(int n) {
	inv[1] = 1;
	for (int i = 2; i <= n; i++) {
		inv[i] = (p - p / i * inv[p % i] % p) % p;
	}
}
```

### 单次复杂度 O(log(mod))
gcd(a, mod)=1

```cpp
int exgcd(int a, int b, int &x, int &y) {
	if(!b) {
		x = 1, y = 0;
		return a;
	}
	int d = exgcd(b, a % b, y, x);
	y -= a / b * x;
	return d;
}

int inv(int a, int mod) {
	int x, y;
	exgcd(a, mod, x, y);
	return (x % mod + mod) % mod;
}
```

---

## 欧拉函数
定义：φ(n) 表示：在 1,2,…,n 中，有多少个数与 n 互质。

算法复杂度：根号 n 

求欧拉函数
```cpp
int phi(int n){
	int ans = n;
	for (int i = 2; i * i <= n; i++) {
		if(n % i == 0) {
			ans = ans / i * (i - 1);
			while(n % i == 0) n /= i;
		}
	}
	if(n > 1) ans = ans / n * (n - 1);
	return ans;
}
```

### 欧拉定理
如果gcd(a,n) = 1, 那么有 a^φ(n) ≡ 1 (mod n)	​

### 费马小定理
a^(p - 1) ≡ 1 (mod p)  p 为质数，a 和 p 互质  

---


## exgcd

普通欧几里得算法用于求：gcd(a,b)  

扩展欧几里得不仅求 gcd，还能找到一组整数 x, y，使得：ax+by=gcd(a,b)	​（裴蜀等式）
也可用于求逆元

求exgcd
ax + by = d  
d = gcd(a, b)
```cpp
int exgcd(int a, int b, int &x, int &y){
	if(!b) {
		x = 1;
		y = 0;
		return a;
	}
	int d = exgcd(b, a % b, y, x);
	y -= a / b * x;
	return d;
}
```

---


## 异或
$$
a \oplus b = (\sim a \;\&\: b) \;|\; (a \;\&\: \sim b)
$$

### 按位与、或、异或互转
$$
\begin{align*}
a \oplus b &= (a \;|\; b) - (a \;\&\: b) \\

\end{align*}
$$

### 二进制加法（进位原理）
$$
a + b = (a \oplus b) + 2 \cdot (a \;\&\: b)
$$

---

## 等差数列

### 一、等差数列 $\{a_n\}$
首项：$a_1$，公差：$d$

1. 通项公式
$$a_n = a_1 + (n-1)d$$

2. 前 $n$ 项和
$$S_n = na_1 + \frac{n(n-1)}{2}d$$

3. 常用性质
若 $m+n=p+q$，则
$$a_m+a_n=a_p+a_q$$

### 二、等比数列 $\{a_n\}$
首项：$a_1$，公比：$q\quad(q\neq0)$

1. 通项公式
$$a_n = a_1 q^{n-1}$$

2. 前 $n$ 项和
$q\neq1$：
$$S_n=\frac{a_1(1-q^n)}{1-q}=\frac{a_1-a_n q}{1-q}$$
$q=1$：
$$S_n = n a_1$$

3. 常用性质
若 $m+n=p+q$，则
$$a_m\cdot a_n=a_p\cdot a_q$$

---

## 线性基
异或线性基

可实现操作：1.将所有异或结果分解为基底，2.求异或和的最大最小值，3.对不同线性基集合进行& 和 | (求交，合并)
```cpp
template<class T>
struct Basis {
    constexpr static int K = 30;
    std::vector<T> e;
    
    Basis() {
        e.assign(K, 0);
    }

    bool add(T x) {
        for (int i = K - 1; i >= 0 && x; i--) {
            if (x >> i & 1) {
                if (e[i]) {
                    x ^= e[i];
                } else {
                    e[i] = x;
                    return true;
                }
            }
        }
        return false;
    }

    T max(T x = 0) {
        for (int i = K - 1; i >= 0; i--) {
            x = std::max(x, x ^ e[i]);
        }
        return x;
    }
    T min(T x) {
        for (int i = K - 1; i >= 0; i--) {
            x = std::min(x, x ^ e[i]);
        }
        return x;
    }

    void work() {
        for (int i = 0; i < K; i++) {
            for (int j = i + 1; j < K; j++) {
                if (e[j] >> i & 1) {
                    e[j] ^= e[i];
                }
            }
        }
    }

    constexpr Basis &operator|=(const Basis &rhs) & {
        for (int i = 0; i < rhs.K; i++) {
            add(rhs.e[i]);
        }
        return *this;
    }
    constexpr Basis &operator&=(const Basis &rhs) & {
        Basis ans;
        std::vector<T> b(rhs.K);
        for (int i = rhs.K - 1; i >= 0; i--) {
            T x = rhs.e[i], v = x;
            for (int k = i; k >= 0 && x; k--) {
                if (x >> k & 1) {
                    if (!e[k]) {
                        e[k] = x;
                        b[k] = v;
                    }
                    x ^= e[k];
                    v ^= b[k];
                }
            }
            ans.add(v);
        }

        *this = std::move(ans);
        return *this;
    }

    constexpr friend Basis operator|(Basis lhs, const Basis &rhs) {
        lhs |= rhs;
        return lhs;
    }
    constexpr friend Basis operator&(Basis lhs, const Basis &rhs) {
        lhs &= rhs;
        return lhs;
    }
};
```

---


## 下取整前缀和公式

求 (x / d下取整)的前缀和
```cpp
auto cal = [&](int x, int d) -> int {
	if(x < 0) return 0;

	int q = x / d;
	int r = x % d;

	return d * q * (q - 1) / 2 + q * (r + 1);
};
```

---

## modint
template<class T>
T power(T a, int b) {
	T res = 1;
	for (; b; b >>= 1, a *= a)
		if(b & 1) res *= a;
	return res;
}

template<int P>
struct MInt {
	int x;

	MInt(int x = 0) : x((x % P + P) % P) {}

	int val() const {
		return x;
	}

	MInt inv() const {
		return power(*this, P - 2);
	}

	MInt operator-() const {
		return MInt(-x);
	}

	MInt &operator+=(const MInt &t) {
		x += t.x;
		if(x >= P) x -= P;
		return *this;
	}

	MInt &operator-=(const MInt &t) {
		x -= t.x;
		if(x < 0) x += P;
		return *this;
	}

	MInt &operator*=(const MInt &t) {
		x = (i128)x * t.x % P;
		return *this;
	}

	MInt &operator/=(const MInt &t) {
		return *this *= t.inv();
	}

	friend MInt operator+(MInt a, const MInt &b) {
		return a += b;
	}

	friend MInt operator-(MInt a, const MInt &b) {
		return a -= b;
	}

	friend MInt operator*(MInt a, const MInt &b) {
		return a *= b;
	}

	friend MInt operator/(MInt a, const MInt &b) {
		return a /= b;
	}

	friend bool operator==(const MInt &a, const MInt &b) {
		return a.x == b.x;
	}

	friend bool operator!=(const MInt &a, const MInt &b) {
		return a.x != b.x;
	}

	friend bool operator<(const MInt &a, const MInt &b) {
		return a.x < b.x;
	}

	friend istream &operator>>(istream &in, MInt &a) {
		int x;
		in >> x;
		a = x;
		return in;
	}

	friend ostream &operator<<(ostream &out, const MInt &a) {
		return out << a.x;
	}
};

using Z = MInt<998244353>;

---

## 二项式定理

1. 定义

二项式定理用于展开二项式的 n 次方：

\[
(a+b)^n=\sum_{k=0}^{n}C_n^k a^{n-k}b^k
\]

其中：

\[
C_n^k=\binom nk=\frac{n!}{k!(n-k)!}
\]

含义：
从 n 个括号中选择 k 个取 b，其余取 a，因此系数为组合数。

---


2. 杨辉三角

组合数满足：

\[
C_n^k=C_{n-1}^{k-1}+C_{n-1}^{k}
\]

杨辉三角第 n 行：

\[
C_n^0,C_n^1,...,C_n^n
\]

---

3. 常用性质

(1) 系数和

令 a=b=1：

\[
(1+1)^n=2^n
\]

所以：

\[
\sum_{k=0}^{n}C_n^k=2^n
\]


(2) 交错和

令 a=1,b=-1：

\[
(1-1)^n=0
\]

所以：

\[
C_n^0-C_n^1+C_n^2-\cdots=0
\]

---

## NTT/FFT

### NTT
数组 0 位存的是常数项
```cpp
const int g = 3;

int ksm(int a, int n) {
	a %= p;
	int res = 1;
	while(n) {
		if(n & 1) {
			res = res * a % p;
		}
		a = a * a % p;
		n >>= 1;
	}
	return res;
}

void ntt(vector<int> &a, int op) {
	int n = a.size();

	for(int i = 1, j = 0; i < n; i++) {
		int bit = n >> 1;
		for(; j & bit; bit >>= 1) {
			j ^= bit;
		}
		j ^= bit;
		if(i < j) {
			swap(a[i], a[j]);
		}
	}

	for(int len = 2; len <= n; len <<= 1) {
		int wlen = ksm(g, (p - 1) / len);
		if(op == -1) {
			wlen = ksm(wlen, p - 2);
		}

		for(int i = 0; i < n; i += len) {
			int w = 1;
			for(int j = 0; j < len / 2; j++) {
				int u = a[i + j];
				int v = a[i + j + len / 2] * w % p;
				a[i + j] = (u + v) % p;
				a[i + j + len / 2] = (u - v + p) % p;
				w = w * wlen % p;
			}
		}
	}

	if(op == -1) {
		int inv = ksm(n, p - 2);
		for(auto &x : a) {
			x = x * inv % p;
		}
	}
}

vector<int> mul(vector<int> a, vector<int> b) {
	int sz = a.size() + b.size() - 1;
	int n = 1;
	while(n < sz) {
		n <<= 1;
	}
	a.resize(n);
	b.resize(n);

	ntt(a, 1);
	ntt(b, 1);
	for(int i = 0; i < n; i++) {
		a[i] = a[i] * b[i] % p;
	}
	ntt(a, -1);
	a.resize(sz);

	return a;
}
```
### FFT
```cpp
const double pi = acos(-1);
using cd = complex<double>;

void fft(vector<cd> &a, int op) {
	int n = a.size();
	for(int i = 1, j = 0; i < n; i++) {
		int bit = n >> 1;
		for(; j & bit; bit >>= 1) {
			j ^= bit;
		}
		j ^= bit;
		if(i < j) {
			swap(a[i], a[j]);
		}
	}

	for(int len = 2; len <= n; len <<= 1) {
		double ang = 2 * pi / len * op;
		cd wlen(cos(ang), sin(ang));
		for(int i = 0; i < n; i += len) {
			cd w(1);
			for(int j = 0; j < len / 2; j++) {
				cd u = a[i + j];
				cd v = a[i + j + len / 2] * w;
				a[i + j] = u + v;
				a[i + j + len / 2] = u - v;
				w *= wlen;
			}
		}
	}

	if(op == -1) {
		for(auto &x : a) {
			x /= n;
		}
	}
}

vector<int> mul(vector<int> a, vector<int> b) {
	int sz = a.size() + b.size() - 1;
	int n = 1;
	while(n < sz) {
		n <<= 1;
	}

	vector<cd> fa(n), fb(n);
	for(int i = 0; i < a.size(); i++) {
		fa[i] = a[i];
	}
	for(int i = 0; i < b.size(); i++) {
		fb[i] = b[i];
	}
	fft(fa, 1);
	fft(fb, 1);
	for(int i = 0; i < n; i++) {
		fa[i] *= fb[i];
	}
	fft(fa, -1);

	vector<int> c(sz);
	for(int i = 0; i < sz; i++) {
		c[i] = (long long)(fa[i].real() + 0.5);
	}
	return c;
}
```

---