## 树的重心
定义：$ f(u)为\text{删除 }u\text{ 后，所有连通块中最大的连通块大小} $

那么使得 \(f(u)\) 最小的节点，就是树的重心

等价性质：一个节点u是重心，当且仅当：删除u后，每个连通块的大小都不超过 n / 2	​

一棵树最多有两个重心，如果有两个重心那么他们一定相邻

本质dfs求子树大小
```cpp
vector<int> center; //所有重心存center里面
int best = inf;
auto dfs = [&](auto &&self, int u, int fa) -> void {
	sz[u] = 1;
	int mx = 0;
	for (int v : g[u]) {
		if(v == fa) continue;
		self(self, v, u);
		sz[u] += sz[v];
		mx = max(mx, sz[v]);
	}
	mx = max(mx, n - sz[u]);

	if(mx < best) {
		best = mx;
		center.clear();
		center.push_back(u);
	}
	else if(mx == best) {
		center.push_back(u);
	}
};
```