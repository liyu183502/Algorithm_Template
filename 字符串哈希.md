```cpp
using ull = unsigned long long;
ull get_hash(string &s)
{
    int n = s.size();
    vector<ull> p(n + 1, 1), h(n + 1, 0);

    for (int i = 1; i <= n; i++) {
        p[i] = p[i - 1] * 131;
        h[i] = h[i - 1] * 131 + s[i - 1];
    }

    return h[n];
}
```