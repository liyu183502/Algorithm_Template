```cpp
void init(){
    fact[1] = 1;
    fact[0] = 1;
    for (int i = 2; i < N; i++) {
        fact[i] = fact[i - 1] * i % p;
    }
    infact[N - 1] = ksm(fact[N - 1], p - 2);
    for (int i = N - 1; i >= 1; i--) {
        infact[i - 1] = infact[i] * i % p;
    }
}
```