```cpp
    int t = 0;
    auto dfs = [&](auto && self, int u) -> bool {
        for (auto v : g[u]){
            if(vis[v] == t) continue;
            vis[v] = t;
            if(ans[v] == 0 || self(self, ans[v])) {
                ans[v] = u;
                return 1;
            }
        }
        return 0;
    };

    for (int i = 1; i <= n; i++) {
        t++;
        if(!dfs(dfs, i)){
            cout << "kou is angry" << endl;
            return 0;
        }
    }
```