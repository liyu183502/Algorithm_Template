```cpp
    vector<int> lis;

    for (int i = 1; i <= n; i++) {
		int x = a[i];
        auto it = lower_bound(lis.begin(), lis.end(), x);

        if(it == lis.end()) {
            lis.push_back(x);
        }
        else {
            *it = x;
        }
    }

    ans += lis.size();
```